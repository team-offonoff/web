import React from 'react';

import { Container, NextButton } from './약관동의.styles';

const 약관동의 = () => {
  const handleNextButton = () => {
    //다음버튼 누르면
  };

  return (
    <Container>
      <NextButton onClick={handleNextButton}>다음</NextButton>
    </Container>
  );
};

export default 약관동의;
